//const express = require("express");
//const router = express.Router();
//const fs = require("fs");
const hostsPath = '../data/printers.json';

function Host() {
    return (
        <div>HOST PAGE</div>
    );
}

export default Host;